# Enable all warnings
enable=all

# Set the severity level for warnings
severity=style

# Enable all warnings
enable=all

# Set the severity level for warnings
severity=style

# Set the shell dialect to POSIX sh
shell=sh

# Include paths for sourcing files
source-path=SCRIPTDIR

# Color output
color=always

# Enable POSIX compliance
enable=posix
